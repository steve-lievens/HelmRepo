apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lisa-backend-nf.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "lisa-backend-nf.name" . }}
    helm.sh/chart: {{ include "lisa-backend-nf.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "lisa-backend-nf.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
